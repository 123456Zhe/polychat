# Copy to keystore.properties for local release builds. Never commit the copy or keystore.
storeFile=../polychat-release.keystore
storePassword=replace-with-secret
keyAlias=polychat
keyPassword=replace-with-secret
